# 0032-ba815-lecture2-slots-clean-fast

- **id:** 0032-ba815-lecture2-slots-clean-fast
- **skill:** researchPrime@2026-08-16
- **situation:** Second scoped example-scan in the same project, same session lineage as 0031 — slot-based
  brief (four named slots, each with a named baseline, keep-baseline first-class).
- **context:** BA 815 revamp, `C:\dev\MBA Teaching AI` — Lecture 2 example slots. Run dir
  `BA 815/Fall 2026/research/rp-lecture2-examples/run`.
- **observation:** (1) **The slot-with-named-baseline brief shape is the one to keep**: every verdict was
  decidable ("beats the trains? no → keep baseline") and one slot honestly returned KEEP BASELINE, which
  the plan had pre-declared as a first-class result — no pressure to force an adopt. (2) **The whole
  pipeline ran in ~20 minutes** vs the first run's ~90: governance recorded programmatically first try
  (0031's lesson applied), searches batched, round-1 findings authored WITH the draft, rounds 2–5
  pre-adjudicated after real fixes. The engine loop cost ~2 min total. (3) Best find was the paired
  Type-I/II dollar exhibit (false declines $443B vs $48B fraud; downtime $600/second) — the round's
  useful work was forcing binding slide rules on the two vendor-sourced figures (single-study
  date-stamp; visible source footer) rather than killing them.
- **outcome:** worked (CLEAN convergence, 5 rounds; single-family stamped; primary sources on the
  non-vendor claims)
- **provenance:** genuine-execution
