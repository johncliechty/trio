id: 0068-live-panel-replies-never-parsed-single-lineage-quorum
skill: researchPrime@2026-09-04
situation: Novelty-claim check for the 3D Parametric Representation campaign (Ecgberht steward; run dir report/researchprime-novelty; tier medium; plan gate APPROVED via the programmatic seam with John's one-line EDIT+APPROVE recorded). First live G3 panel spawned cleanly on Gemini seats (per-run ANCHOR_DATA_DIR override coding=claude/review=gemini, because the artifact was Claude-authored and prefs would have had Claude review Claude).
context: The engine reported round 1 as EMPTY (three seats, zero findings, dry=true empty=true). The seats' own transcripts (Gemini CLI brain dirs) held SEVEN findings in ```json-fenced objects: C7 high from all three seats, C6 medium from two, C10 and CLAIM-0 medium from one each. The Contrarian's last heartbeat even said "Outputting findings JSON".
observation: THREE defects, in order of consequence. (1) The live panel path read `out?.findings` on the driver's `{ text, rec }` reply, so a fenced JSON reply ALWAYS parsed as zero findings, and an unparsed reply was indistinguishable from a clean look; journal 0067's "3/3 seats returned 0 findings" was almost certainly the same defect, not a clean artifact. Fixed in bin/round.mjs: `extractPanelFindings` (whole-string fence, last fenced block, bare JSON, brace-scan for the last object holding "findings"), an unparseable reply now ABSTAINS (answerable:'no', panel_fault) instead of counting as surviving-with-nothing, and `raw_reply` is kept on each review for audit; test/panel-reply-parse.test.mjs (7 tests) pins it. Upstream, Crucible's agent seam "schema reply" tests are RED on main (4 failures: fenced JSON parsed, retry once, abstain after retry) — pre-existing, untouched by this change, and the likely root of the same symptom in every skill that relies on the drivers' schema replies. (2) A single-lineage panel can never mint a blocker: GATE-1 counts origins, all three seats were gemini, so quorum={origins:1,required:2,met:false} demoted every finding and the round converged "dry" with the Judge saying NOT_CONVERGED; the honesty guard did not fire (singleFamily:null). The engine needs a stamp "all findings demoted: single lineage" that blocks a DRY convergence claim, or a second-family seat by default. Worked around here with an independent ChatGPT-family reviewer (codex exec, read-only) added to the round input. (3) Replay mode ignores the spawned-reviews sidecar unless RESEARCHPRIME_LIVE_ROUND=1; reviews must be on the round input to replay. Operator notes: the gate records must sit in the run dir ROOT (a subfolder halts "governance.json missing"); `rm` is blocked in the steward host, archive with `mv`.
outcome: friction — the loop is being finished with the recovered findings, a v2 ledger, and a second live round; the deliverable will carry the parse-defect and single-lineage stamps explicitly. Elegance rule 9: the parser fix is promoted to code + tests, not left here.
provenance: genuine-execution
