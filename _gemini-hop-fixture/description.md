# Description — Gemini-host hop fixture

A minimal one-wave project used ONLY to prove a Foreman spawn+gate hop runs with the
sub-agents driven by `gemini -p` (Gemini as host). The deliverable is a trivial pure
function so the run exercises the orchestration path (execute -> orchestrator gate ->
review), not domain difficulty.
