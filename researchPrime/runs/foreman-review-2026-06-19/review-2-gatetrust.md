# REVIEW 2 — gate-trust / merge-cost realist (adversarial, code-grounded)

- Attack 1 REFUTES Branch 3 as justified: best-of-N (Large Language Monkeys) needs a SOUND verifier. Foreman's gate (runGate wave-engine.mjs:548-664) is "only as strong as the plan's tests"; vacuous-GREEN/import-reachability self-describes as "coverage PROXY only" (line 395), "full coverage = Phase 3 hardening" (224). Gate cannot RANK two passing candidates (judge:701 returns boolean GO, no tie-break) -> picking a gate-passer SILENTLY commits arbitrary design drift across waves. NEW: anti-test-weakening (checkTestWeakening:158) + test-immutability (checkTestImmutability:206) are PER-INVOCATION baselines snapshotted post-execute (804) assuming a SINGLE author; fan-out breaks "whose test set is the immutable baseline?" -> structural conflict with 2 of 3 integrity guards.
- Attack 2 REFUTES Branch 1 premise: grep worktree bin/ = NOTHING. Git model (git-hygiene.mjs) is the opposite of mergeable: one work branch, never -B, per-file staging never git add -A, refuses >1 toplevel, dirty-tree HALT, commit-only-on-GO, crash-reconcile assumes SINGLE linear commit stream. Branch 1 needs worktree+branch-per-unit+merge+merge-HALT+RE-GATE-after-merge (merged tree is a config NO sub-agent produced + NO gate verified). Conflict RELOCATED to merge time, not removed; can produce a passing gate over an incoherent design = strictly worse than serial.
- Attack 3 REFUTES fan-out economics (strongest): §10 "harness exposes no live quota API" (foreman-lib.mjs:418); window status is REACTIVE from observed 429s (rear-view). NO pre-flight rate gate (canStartWave:501/canStartFixIter:515 check only wall-clock+wave caps). Fan-out burns the window N× faster, finds out only by 429-mid-flight. Single Pro/Max window is per-SUBSCRIPTION not per-thread; §11 calibration ~1-3 waves/window is the binding limit. Threads cannot beat a per-subscription window. No concurrency limiter exists.
- Attack 4 SURVIVES (concurrent reviewers SAFE) but low value: VERIFIED collectFindings:678-694 merges by content-stable id (file:line+rule) into a Map, agreement = reviewers.size (Set union) -> ORDER-INDEPENDENT by construction. (This REFUTES Review-1 Attack-4 #2.) But REVIEW (default 2 read-only) is a non-bottleneck; EXECUTE + FIX*MAX_ITERS(4) dominate. Amdahl trimming. Ship because harmless, not impactful.
- Attack 5 REFUTES "prevent": write-set known only post-EXECUTE (git.changedVsHead inside gate loop:836). Disjoint-write-set gate = detect-and-HALT at merge, after N agents already ran/spent tokens. Serial EXECUTE makes overlap STRUCTURALLY IMPOSSIBLE for free. Detect-and-HALT that costs N× to find a conflict the current design can't have = regression.

## Ranked verdict
SHIP: (1) concurrent reviewers (Promise.all 859-863) — only unambiguous win, low value. (5) reword SKILL.md to shared-mutation — safe ONLY as descriptive doc, must NOT become a license for fan-out/merge.
TRAPS (do not ship): (3) speculative fan-out — fights gate-soundness + breaks immutability baselines + worst economic offender. (2) worktree+merge — invents a concurrency subsystem against single-branch hygiene, relocates conflict to merge.
Already-correct: (4) keep pipelining out (same logic should kill Rec 2).

## Core error identified
Synthesis treats Foreman's orchestrator-run gate as a SOUND verifier; the code self-describes it as forgery-resistant but coverage-PROXY — strong enough to trust ONE run, far too weak to arbitrate best-of-N or a post-merge tree.
