id: 0066-panel-readonly-seats-cannot-heartbeat
skill: researchPrime@2026-09-01
situation: Correction to 0065 (append-only). Same run (Help Sam / Dallon + MATH 636), attempt 2 relaunched with cwd = runDir per 0065's hypothesis; all three seats died again as "silent" at 180 s, no heartbeat file, no halt for 270 s+ (respawn cycle).
context: Process table during attempt 2 showed the real seat command line: `claude.exe -p " " --output-format stream-json --verbose --permission-mode plan --allowedTools Read,Glob,Grep` — drivers/claude.mjs forces every verification role (reviewer/shark/debate/judge) to PLAN mode with read-only tools (the no-shell-from-agents rule). A manual seat run from the run dir with acceptEdits + Write allowed wrote its heartbeat and answered in 12 s.
observation: ROOT CAUSE (verified, retires 0065's cwd hypothesis): the look-in law (swarm-lookin.mjs lookinAppendix: "you MUST write <heartbeat> … silence is death") and the verification-seat policy (plan mode, Read/Glob/Grep only) are mutually exclusive. A read-only seat can never write the trail the supervisor keys on, so verdictFromTrail sees idle >= silentKillMs and kills every seat before its stdout reply arrives; the respawn dies the same way. Live spawned panels therefore cannot converge on this driver unless the seat finishes inside silentKillMs (180 s in run-rounds), which a real read of a 12 KB artifact does not.
outcome: friction — worked around for this run with the existing env knobs: RESEARCHPRIME_PANEL_SILENT_KILL_MS=900000, RESEARCHPRIME_PANEL_DEAD_MS=900000, RESEARCHPRIME_PANEL_CHECKIN_MS=120000 (attempt 3; the 20-min CLAUDE_CALL_TIMEOUT_MS still bounds each seat). Mechanism fix to promote (Elegance rule 8): either (a) make the supervisor count stream-json assistant/tool events as liveness for read-only seats, or (b) let verification seats Write ONLY the heartbeat path (acceptEdits + allowedTools Read,Glob,Grep,Write with cwd = runDir; still no Bash). Until then the operator recipe must say: live panel needs the three PANEL_* env knobs raised, or it will always HALT "silence is death".
provenance: genuine-execution
