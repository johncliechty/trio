# 0044 — P2: dark-module reaping + the bin-reachability lint + facet-coverage restored (2026-07-25)

- Archived to `_archive/dark-modules-2026-07-25/`: `governance-record.mjs` (the
  snake_case dead twin whose authoritative-sounding validator was a trap) and
  `calibrate-shadow.mjs` (test-only; the live calibration path is calibrationVerdict,
  wired since 0043). `facet-coverage.mjs` went too — then came BACK the same day: the
  lit-review dual-suite fence asserts its presence (it is the STAGED seam for the
  2026-07-21 2D breadth-scoping effort). Now allowlisted with that reason.
- **`test/bin-reachability.test.mjs`** — the anti-dark-module lint: every bin/*.mjs
  must be reachable from run-rounds/plan-gate or allowlisted WITH a reason. This is
  the check whose absence let T9 survive two portfolio reviews.
- **`bin/WAVE-NUMBERING.md`** — the two-programs-one-directory map; module headers
  citing bare wave numbers is how "Wave 8 GOVERNOR WIRING" read as done while a
  different program's wave-8 test was green.
- Ripple: the lit-review RP golden fence flagged the 0043 plan-gate contract change
  (the golden had pinned the mock-hash BUG) — goldens re-recorded there.

Suite: 197/1 (the 1 = the pre-existing trio-green foreman cascade).

provenance: genuine-execution (direct-fix session, tests green)
