id: 0065-dallon-math636-panel-cwd-halt
skill: researchPrime@2026-09-01
situation: Low-tier live run on "John Dallon as a teacher + BYU MATH 636 (Fall 2026)" for Help Sam; plan gate APPROVED via the programmatic seam (non-TTY host), artifact FULL-REPORT.md with claim ids, round-1-input with vacant reviews[] to spawn the G3 panel (review_family=claude).
context: Engine invoked as `node C:/dev/trio/researchPrime/bin/run-rounds.mjs <runDir>` with process cwd = C:/dev/trio/researchPrime (the skill folder), runDir under C:/dev/Help Sam. Gate records had to be COPIED from plan-gate-run/ into the run root because loadGate/loadTriageExtensionFromRunDir read the round-input directory only.
observation: All three seats died at exactly silentKillMs (180009 / 180010 ms; one respawn reached 270015 ms) with "no response — silence is death"; no heartbeat-r1-*.json ever appeared; no orphan claude.exe left. A direct `claude -p` ping from inside the session answered in 5-6 s (nested CLAUDECODE env is NOT the cause). Root cause: the claude driver spawns seats with cwd = process.cwd() and --permission-mode acceptEdits; the seat's mandatory heartbeat Write to <runDir>/heartbeat-r1-<role>.json (and the artifact Read) were OUTSIDE that cwd, so they were refused without any stdout signal, and the supervisor — whose only liveness input is the heartbeat trail's mtime — killed every seat as silent. The same cwd-allowlist mechanism recorded for Foreman in-session launches.
outcome: friction — attempt 2 relaunched with cwd = runDir (heartbeat + artifact inside scope), HALT-RECORD.json archived as HALT-RECORD-attempt1.json. Fix to promote: run-rounds should pass target=runDir (or --add-dir runDir) to the live agent instead of inheriting process.cwd(), and the operator recipe should say "launch FROM the run dir". Also: the SKILL.md probe path `../../drivers/index.mjs` only resolves through the real trio path, not the ~/.claude/skills junction.
provenance: genuine-execution
