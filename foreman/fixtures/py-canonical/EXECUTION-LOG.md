# Pocket Calculator (Python) — Execution Log

This log belongs to the Python canonical fixture project. It starts essentially
empty: the engine appends its execute/review/fix/gate entries here as it runs the
fixture. It exists now so `locateDocs` resolves all three frozen docs without
ambiguity.

---

(no engine runs yet — the fixture ships in its initial "red" state with the
planted subtraction bug present)
